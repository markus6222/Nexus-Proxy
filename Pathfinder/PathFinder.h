#pragma once
// The pathfinder this project uses already lives in PathFinder_2.h, where the
// class is named PathFinderxd. Call sites expect the name PathFinder.
#include "../PathFinder_2.h"

using PathFinder = PathFinderxd;
